# Properties file with credentials that the project primarily requires for publishing.
# The file is only a placeholder and is not maintained in SCM.
# The data can be entered here directly or generated dynamically with the start of the IDE.
docker.registry = docker.com
docker.user     = <user>
docker.password = <password>
